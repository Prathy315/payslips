import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { Profile } from '../../components/EMS/Profile'

export default function profile() {
  return (
   <Profile/>
  )
}

const styles = StyleSheet.create({})